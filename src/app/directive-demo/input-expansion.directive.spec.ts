import { InputExpansionDirective } from './input-expansion.directive';

describe('InputExpansionDirective', () => {
  it('should create an instance', () => {
    const directive = new InputExpansionDirective();
    expect(directive).toBeTruthy();
  });
});
